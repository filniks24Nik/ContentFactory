# Phase-1 dependencies — PINNED to versions verified working in CI (rule 10: reproducible env).
# Bump deliberately + re-test; don't let a transitive update silently break the daily pipeline.

# LLM (scriptwriter + ideation, grounded + fallback)
google-genai==2.8.0             # Gemini — current SDK (`from google import genai`)
groq==1.4.0                     # Groq failover

# Database / state
supabase==2.31.0               # Supabase Postgres client

# Approval UI
# NOTE: approval.py uses the Telegram Bot HTTP API directly via `requests` (below) — no async framework.

# Voice (Module 4)
kokoro-onnx==0.5.0             # primary humanized TTS (Apache-2.0, CPU/ONNX; int8 model auto-downloads)
edge-tts==7.2.8               # narration fallback (free, no key)
# NOTE: Kokoro needs espeak-ng on Linux for phonemization — install it in CI (apt-get).

# Video (Modules 5, 6, 7)
# NOTE: assembly/visuals call the FFmpeg *binary* directly via subprocess (no Python wrapper) —
# install FFmpeg as a system dep (winget Gyan.FFmpeg / apt). On CI, install it onto PATH.
faster-whisper==1.2.1         # word-level timestamps for karaoke captions
numpy==2.4.6                  # Kokoro PCM handling (also a transitive dep)
pillow==12.2.0                # graphics.py stat-card PNGs (ENABLE_GRAPHIC_CARDS overlay path)

# Publishing (Module 9) + analytics (Module 10)
google-api-python-client==2.197.0  # YouTube Data API v3
google-auth==2.53.0                # OAuth for headless upload
google-auth-oauthlib==1.4.0        # one-time local OAuth flow

# Misc
requests==2.34.2              # Pexels/Pixabay/Cloudflare/Telegram HTTP, trends RSS
python-dotenv==1.2.2          # load local .env in dev
