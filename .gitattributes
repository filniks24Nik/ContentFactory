# Normalize line endings: LF in the repo, native on checkout.
# Dev is Windows; the pipeline runs on GitHub Actions (Linux). This kills noisy diffs.
* text=auto eol=lf

# Treat media as binary (also gitignored, but belt-and-suspenders).
*.mp4 binary
*.wav binary
*.mp3 binary
*.png binary
*.jpg binary
